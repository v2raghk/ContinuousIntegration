name: Exam CI

on:
  push:
    branches: [ "main" ]

jobs:
  check-exam:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check file exists
        run: test -f exam.html

      - name: Check file not empty
        run: test -s exam.html

      - name: Check content
        run: |
          grep -q "Submit Answer" exam.html || exit 1
          grep -q "questions" exam.html || exit 1